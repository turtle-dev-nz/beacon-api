PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/business_cards

# Google Gemini API key (server-side only — never expose to the client)
GEMINI_API_KEY=

# Populated by: npm run seed
DEV_USER_ID=

# Directory for uploaded card images (relative to apps/api)
UPLOADS_DIR=uploads
